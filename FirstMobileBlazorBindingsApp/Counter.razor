<Frame CornerRadius="10" BackgroundColor="Color.Red">

    <StackLayout Orientation="StackOrientation.Horizontal" HorizontalOptions="LayoutOptions.Center">
        <Label TextColor="Color.Black" Text="Todos por Alex....."/>
    </StackLayout>
</Frame>

<Frame CornerRadius="10" BackgroundColor="Color.LightBlue">

    <StackLayout Orientation="StackOrientation.Horizontal" HorizontalOptions="LayoutOptions.Center">

        <Button  Text="Increment" OnClick="@IncrementCount" />

        @*<Label  Text="@("The button was clicked " + count + " times")" FontAttributes="FontAttributes.Bold"
        VerticalTextAlignment="TextAlignment.Center" />*@
        <Label Text="@($"The button was clicked {count} times")" FontAttributes="FontAttributes.Bold"
               VerticalTextAlignment="TextAlignment.Center" />

    </StackLayout>

</Frame>

@code
{
    int count;

    void IncrementCount()
    {
        count += 55; ;
    }
}
